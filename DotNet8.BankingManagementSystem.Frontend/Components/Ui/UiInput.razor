@namespace DotNet8.BankingManagementSystem.Frontend.Components.Ui

<input class="@ComputedClass"
       type="@Type"
       value="@Value"
       placeholder="@Placeholder"
       disabled="@Disabled"
       @oninput="OnInput" />

@code {
    [Parameter] public string Type { get; set; } = "text";
    [Parameter] public string? Value { get; set; }
    [Parameter] public EventCallback<ChangeEventArgs> ValueChanged { get; set; }

    [Parameter] public string? Placeholder { get; set; }
    [Parameter] public bool Disabled { get; set; }
    [Parameter] public string? Class { get; set; }

    private string ComputedClass => ClassNames.Merge("ui-input", Disabled ? "ui-disabled" : null, Class);

    private Task OnInput(ChangeEventArgs e) => ValueChanged.InvokeAsync(e);
}

