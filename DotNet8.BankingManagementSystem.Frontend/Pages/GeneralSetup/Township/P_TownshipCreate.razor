@page "/general-setup/township/create"

<EditForm Model="@_model" OnValidSubmit="OnValidSubmit">
    <MudGrid Class="justify-content-center">
        <MudItem xs="12" sm="7">
            <MudText Typo="Typo.h6" Class="mud-width-full">Township Create Form</MudText>
            <MudCard>
                <MudCardContent>
                    <MudTextField Label="Township Code"
                                  @bind-Value="_model.TownshipCode" For="@(() => _model.TownshipCode)"/>
                    <MudTextField Label="Township Name" Class="mt-3"
                                  @bind-Value="_model.TownshipName" For="@(() => _model.TownshipName)"/>
                    <MudSelect T="string" @bind-Value="_model.StateCode" Class="pt-3" Label="State" AnchorOrigin="Origin.BottomCenter" Clearable>
                        @if (_stateListResponseModel is not null && _stateListResponseModel.Data is not null)
                        {
                            @foreach (var item in _stateListResponseModel.Data)
                            {
                                <MudSelectItem Value="@item.StateCode">@item.StateName</MudSelectItem>
                            }
                        }
                    </MudSelect>
                </MudCardContent>
                <MudCardActions>
                    <MudButton ButtonType="ButtonType.Button" Variant="Variant.Filled" Href="/general-setup/township" Color="Color.Warning">
                        Cancel
                    </MudButton>
                    <MudButton ButtonType="ButtonType.Submit" Variant="Variant.Filled" Color="Color.Primary" Class="ml-2">Create Township</MudButton>
                </MudCardActions>
            </MudCard>
        </MudItem>
    </MudGrid>
</EditForm>