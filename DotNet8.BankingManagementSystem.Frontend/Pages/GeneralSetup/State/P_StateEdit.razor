@page "/general-setup/state/edit/{stateCode}"

@if (_model is not null)
{
    <div class="max-w-xl mx-auto space-y-8 animate-in fade-in slide-in-from-bottom-2 duration-500">
        <div class="space-y-2">
            <h1 class="text-2xl font-bold tracking-tight">Edit State</h1>
            <p class="text-muted-foreground text-sm">Update geographic information for <span class="font-mono font-bold text-foreground">@stateCode</span>.</p>
        </div>

        <UiCard>
            <EditForm Model="@_model" OnValidSubmit="OnValidSubmit">
                <DataAnnotationsValidator />
                
                <div class="p-6 space-y-6">
                    <!-- Inputs Group -->
                    <div class="grid grid-cols-1 gap-4">
                        <div class="space-y-2">
                            <UiLabel>State Code</UiLabel>
                            <div class="ui-input bg-muted/50 text-muted-foreground cursor-not-allowed select-none flex items-center px-3 h-10 rounded-md border border-input text-sm font-mono">
                                @_model.StateCode
                            </div>
                        </div>

                        <div class="space-y-2">
                            <UiLabel>State Name</UiLabel>
                            <InputText @bind-Value="_model.StateName" 
                                       class="ui-input" 
                                       placeholder="e.g. Yangon" />
                            <ValidationMessage For="@(() => _model.StateName)" class="text-[10px] text-destructive font-medium" />
                        </div>
                    </div>
                </div>

                <div class="flex items-center justify-end gap-3 p-6 pt-0">
                    <UiButton Type="button" Variant="UiButtonVariant.Ghost" OnClick="@(() => NavigationManager.NavigateTo("/general-setup/state"))">
                        Cancel
                    </UiButton>
                    <UiButton Type="submit" Variant="UiButtonVariant.Default">
                        Save Changes
                    </UiButton>
                </div>
            </EditForm>
        </UiCard>
    </div>
}
