@page "/user-and-account/admin-user"

@if (_model is not null && _model.Data is not null)
{
    <div class="space-y-6 animate-in fade-in slide-in-from-bottom-2 duration-500">
        <UiPageHeader Title="Admin Staff" Subtitle="Manage system administrators and staff access roles.">
            <Actions>
                <div class="relative w-full md:w-64 group">
                    <i class="fa-solid fa-magnifying-glass absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground text-xs"></i>
                    <input type="search" 
                           class="ui-input pl-9 h-9 text-xs" 
                           placeholder="Search staff..."/>
                </div>
                <UiButton Variant="UiButtonVariant.Default" Size="UiButtonSize.Sm" OnClick="@(() => NavigationManager.NavigateTo("/user-and-account/admin-user/create"))">
                    <i class="fa-solid fa-user-plus mr-2 opacity-70"></i>
                    Add Staff
                </UiButton>
            </Actions>
        </UiPageHeader>

        <UiCard>
            <div class="p-0 overflow-auto">
                <table class="ui-table">
                    <thead class="ui-table-header">
                        <tr class="ui-table-row">
                            <th class="ui-table-head w-16 text-center">#</th>
                            <th class="ui-table-head">Staff Details</th>
                            <th class="ui-table-head">Contact</th>
                            <th class="ui-table-head">Role</th>
                            <th class="ui-table-head text-right pr-6">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{ int count = 0; }
                        @foreach (var item in _model.Data)
                        {
                            <tr class="ui-table-row group">
                                <td class="ui-table-cell text-center text-xs text-muted-foreground">
                                    @(++count + ((_model.PageSetting?.PageNo - 1) * _model.PageSetting?.PageSize))
                                </td>
                                <td class="ui-table-cell">
                                    <div class="flex items-center gap-3">
                                        <div class="w-8 h-8 rounded bg-muted flex items-center justify-center text-[10px] font-bold">
                                            @item.AdminUserName.Substring(0, 1).ToUpper()
                                        </div>
                                        <div class="flex flex-col">
                                            <span class="text-xs font-bold">@item.AdminUserName</span>
                                            <span class="text-[10px] text-muted-foreground font-mono">@item.AdminUserCode</span>
                                        </div>
                                    </div>
                                </td>
                                <td class="ui-table-cell">
                                    <div class="flex items-center gap-2 text-xs">
                                        <i class="fa-solid fa-phone text-[10px] text-muted-foreground"></i>
                                        @item.MobileNo
                                    </div>
                                </td>
                                <td class="ui-table-cell">
                                    <span class="ui-badge ui-badge-secondary text-[10px] font-bold uppercase tracking-tight">
                                        @item.UserRoleCode
                                    </span>
                                </td>
                                <td class="ui-table-cell text-right pr-4">
                                    <div class="flex justify-end gap-1">
                                        <UiButton Variant="UiButtonVariant.Ghost" Size="UiButtonSize.Icon" 
                                                  OnClick="@(() => NavigationManager.NavigateTo($"/user-and-account/admin-user/edit/{item.AdminUserCode}"))">
                                            <i class="fa-solid fa-pen-to-square text-xs opacity-70"></i>
                                        </UiButton>
                                        <UiButton Variant="UiButtonVariant.Ghost" Size="UiButtonSize.Icon" 
                                                  Class="hover:bg-destructive/10 hover:text-destructive"
                                                  OnClick="() => Delete(item.AdminUserCode)">
                                            <i class="fa-solid fa-trash text-xs opacity-70"></i>
                                        </UiButton>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            <div class="p-4 border-t border-border flex flex-col sm:flex-row items-center justify-between gap-4 bg-muted/10">
                <div class="text-[11px] text-muted-foreground font-medium">
                    Showing page <span class="text-foreground font-bold">@_model.PageSetting.PageNo</span> of <span class="text-foreground font-bold">@_model.PageSetting.PageCount</span>
                </div>
                <UiPagination PageNo="@(_model.PageSetting.PageNo)" 
                               PageCount="@(_model.PageSetting.PageCount)" 
                               SelectedChanged="PageChanged" />
            </div>
        </UiCard>
    </div>
}
